apiVersion: 1

providers:
  - name: 'PDF Redaction Dashboards'
    orgId: 1
    folder: 'PDF Redaction'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
